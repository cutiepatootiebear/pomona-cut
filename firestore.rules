rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Test mode: open until after the Aug 7 finish line
      allow read, write: if request.time < timestamp.date(2026, 9, 1);
    }
  }
}
